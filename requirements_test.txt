# Additional testing dependencies
#
# Install with: uv pip install -r requirements_test.txt

pytest>=9.0.3
pytest-asyncio>=1.3.0
pytest-aiohttp>=1.1.0
pytest-cov>=7.1.0
pytest-timeout>=2.4.0
# parallel test execution
pytest-xdist>=3.8.0
coverage>=7.13.5
# time mocking
freezegun>=1.5.5
requests-mock>=1.12.1
# httpx mocking
respx>=0.23.1

# HTTPX is a next-generation HTTP client for Python.
httpx>=0.28.1

# Authlib enables OAuth 1.0 and OAuth 2.0 for HTTPX with its async versions:
authlib>=1.7.0

# python-dotenv reads key-value pairs from a .env file and can set them as environment variables.
python-dotenv>=1.2.2
